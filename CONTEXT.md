# CONTEXT.md — 领域词汇表

只放术语定义，不放实现细节。

## 上下文压缩（Compaction）

- **自动压缩（Auto Summarization）**：模型上下文占用达到触发阈值时，框架自动把较早的对话历史摘要掉的行为。
- **手动压缩（Manual Compaction）**：用户主动用命令或按钮触发的压缩。**不看触发阈值**——要不要压由用户决定，压完留多少由 keep 策略决定。
- **触发阈值（Trigger）**：自动压缩的启动条件（如占模型窗口的 90%）。只约束自动压缩，对手动压缩无效。
- **keep 策略（Keep）**：压缩时保留最近多少对话的规则（比例或条数）。手动压缩默认沿用配置里的 keep，可临时覆盖。
- **压缩分界线（Compaction Boundary）**：聊天记录里的一条分隔显示，标明"这条线以上的消息已进摘要，模型只记得摘要；以下的消息模型完整记得"。
- **水位条（Context Meter）**：当前窗口 token 占用与模型 context_limit 的比值可视化。
